{
	"name": "@sikessem/ui",
	"version": "0.0.0",
	"imports": {
		"npm:colorwind": "jsr:@siguici/colorwind"
	},
	"exports": {
		".": "./jsr/index.ts"
	},
	"include": ["jsr.json", "jsr/*.ts", "README.md", "LICENSE.md"],
	"exclude": [
		".*",
		"jsr/*.test.ts",
		"js/*",
		"ts/*",
		"src/*",
		"tests/*",
		"biome.json",
		"tsconfig.json",
		"deno.lock"
	],
	"publish": {
		"exclude": ["!jsr/*.ts"]
	},
	"fmt": {
		"useTabs": true,
		"lineWidth": 80,
		"indentWidth": 2,
		"semiColons": true,
		"singleQuote": true,
		"proseWrap": "preserve"
	},
	"lint": {
		"include": ["jsr/"],
		"exclude": ["!jsr/*.ts", "!jsr/*.test.ts"],
		"rules": {
			"tags": ["recommended"]
		}
	},
	"tasks": {
		"check": "deno task check:fmt && deno task check:lint && deno task check:types",
		"check:fmt": "deno fmt --check",
		"check:lint": "deno lint",
		"check:types": "deno check jsr/*.ts",
		"fix": "deno lint --fix && deno fmt"
	}
}
